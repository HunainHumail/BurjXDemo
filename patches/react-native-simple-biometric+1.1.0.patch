diff --git a/node_modules/react-native-simple-biometric/android/src/main/java/com/simplebiometric/BiometricAuthModule.kt b/node_modules/react-native-simple-biometric/android/src/main/java/com/simplebiometric/BiometricAuthModule.kt
index 3e2dab4..1291435 100644
--- a/node_modules/react-native-simple-biometric/android/src/main/java/com/simplebiometric/BiometricAuthModule.kt
+++ b/node_modules/react-native-simple-biometric/android/src/main/java/com/simplebiometric/BiometricAuthModule.kt
@@ -1,4 +1,5 @@
-package com.simplebiometric;
+// BiometricAuthModule.kt (full corrected version)
+package com.simplebiometric
 
 import android.os.Build
 import android.os.Handler
@@ -8,7 +9,6 @@ import androidx.biometric.BiometricPrompt
 import androidx.core.content.ContextCompat
 import androidx.fragment.app.FragmentActivity
 import com.facebook.react.bridge.*
-import java.util.concurrent.Executor
 
 class BiometricAuthModule(reactContext: ReactApplicationContext) : ReactContextBaseJavaModule(reactContext) {
 
@@ -29,14 +29,24 @@ class BiometricAuthModule(reactContext: ReactApplicationContext) : ReactContextB
         mainHandler.post {
             try {
                 val biometricManager = BiometricManager.from(reactApplicationContext)
-                val canAuthenticate = biometricManager.canAuthenticate()
+                val authenticators = if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.R) {
+                    BiometricManager.Authenticators.BIOMETRIC_STRONG
+                } else {
+                    BiometricManager.Authenticators.DEVICE_CREDENTIAL
+                }
+
+                val canAuthenticate = if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.R) {
+                    biometricManager.canAuthenticate(authenticators)
+                } else {
+                    biometricManager.canAuthenticate()
+                }
 
                 when (canAuthenticate) {
                     BiometricManager.BIOMETRIC_SUCCESS -> {
                         if (isFaceRecognitionAvailable()) {
-                            promise.resolve("FaceRecognition")
+                            promise.resolve("FaceID")
                         } else {
-                            promise.resolve("Fingerprint")
+                            promise.resolve("TouchID")
                         }
                     }
                     BiometricManager.BIOMETRIC_ERROR_NONE_ENROLLED -> promise.resolve("NoBiometricsEnrolled")
@@ -53,21 +63,28 @@ class BiometricAuthModule(reactContext: ReactApplicationContext) : ReactContextB
     fun authenticate(promise: Promise) {
         mainHandler.post {
             try {
+                val activity = currentActivity as? FragmentActivity
+                    ?: throw IllegalStateException("No active fragment activity")
+
                 val biometricManager = BiometricManager.from(reactApplicationContext)
-                val canAuthenticate = biometricManager.canAuthenticate()
+                val authenticators = if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.R) {
+                    BiometricManager.Authenticators.BIOMETRIC_STRONG
+                } else {
+                    BiometricManager.Authenticators.DEVICE_CREDENTIAL
+                }
 
-                if (canAuthenticate != BiometricManager.BIOMETRIC_SUCCESS) {
-                    promise.reject(BIOMETRIC_NOT_AVAILABLE, "Biometric authentication is not available")
-                    return@post
+                val canAuthenticate = if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.R) {
+                    biometricManager.canAuthenticate(authenticators)
+                } else {
+                    biometricManager.canAuthenticate()
                 }
 
-                val activity = currentActivity as? FragmentActivity
-                if (activity == null) {
-                    promise.reject(ACTIVITY_ERROR, "The current activity is null")
+                if (canAuthenticate != BiometricManager.BIOMETRIC_SUCCESS) {
+                    promise.reject(BIOMETRIC_NOT_AVAILABLE, "Biometric authentication unavailable")
                     return@post
                 }
 
-                val executor: Executor = ContextCompat.getMainExecutor(reactApplicationContext)
+                val executor = ContextCompat.getMainExecutor(reactApplicationContext)
 
                 biometricPrompt = BiometricPrompt(activity, executor,
                     object : BiometricPrompt.AuthenticationCallback() {
@@ -86,7 +103,15 @@ class BiometricAuthModule(reactContext: ReactApplicationContext) : ReactContextB
 
                 val promptInfo = BiometricPrompt.PromptInfo.Builder()
                     .setTitle("Authentication Required")
-                    .setDescription("Authenticate with your biometric credential")
+                    .setSubtitle("Use biometric to continue")
+                    .apply {
+                        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.R) {
+                            setAllowedAuthenticators(BiometricManager.Authenticators.BIOMETRIC_STRONG)
+                        } else {
+                            @Suppress("DEPRECATION")
+                            setDeviceCredentialAllowed(false)
+                        }
+                    }
                     .setNegativeButtonText("Cancel")
                     .build()
 
@@ -97,12 +122,12 @@ class BiometricAuthModule(reactContext: ReactApplicationContext) : ReactContextB
         }
     }
 
-    private fun isFaceRecognitionAvailable(): Boolean {
-        return Build.VERSION.SDK_INT >= Build.VERSION_CODES.Q
+    override fun invalidate() {
+        biometricPrompt = null
+        super.invalidate()
     }
 
-    override fun onCatalystInstanceDestroy() {
-        super.onCatalystInstanceDestroy()
-        biometricPrompt = null
+    private fun isFaceRecognitionAvailable(): Boolean {
+        return Build.VERSION.SDK_INT >= Build.VERSION_CODES.Q
     }
 }
\ No newline at end of file
